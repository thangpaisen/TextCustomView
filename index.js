/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import ExampleTextCustomView from './src/example/index';

AppRegistry.registerComponent(appName, () => ExampleTextCustomView);
